# 🔍 SonarQube project identity
sonar.projectKey=fulldevops
sonar.projectName=Full  Devops Project
sonar.projectVersion=1.0
 
# 📂 Source & Binary Paths
sonar.sources=demo-app/src
sonar.java.binaries=demo-app/target/classes
 
# 🔐 Authentication (optional if token is injected by Jenkins)
 sonar.login=sqa_ab21582b16ee5a11f256b8b9211d3918f438e027
#   ← avoid hardcoding in real-time
 
# 🌐 SonarQube server URL (optional if Jenkins injects it)

sonar.host.url=http://localhost:9000

 
# 🛑 Optional exclusions (e.g. test files, configs)
# sonar.exclusions=**/test/**,**/config/**
 
# 🧪 Coverage report location (if using JaCoCo later)
# sonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
